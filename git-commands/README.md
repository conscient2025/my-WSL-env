查看 git 状态：

```bash
git status
```

确认远程仓库：

```bash
git remote -v
```

把已经被跟踪的文件/目录从索引中移除：

```bash
git rm -r --cached <dir/file>
```

查看改动

```bash
git diff
```

查看提交历史

```bash
git log --oneline
```
